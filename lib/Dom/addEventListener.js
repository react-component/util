var addDOMEventListener = require('add-dom-event-listener');

addDOMEventListener = addDOMEventListener.default ? addDOMEventListener.default : addDOMEventListener;

module.exports = addDOMEventListener;
