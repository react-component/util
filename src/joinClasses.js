import deprecate from 'util-deprecate';
import classNames from 'classnames';

export default deprecate(classNames, '`rcUtil.joinClasses()` is deprecated, use `classNames()` by `require(\'classnames\')` instead');
